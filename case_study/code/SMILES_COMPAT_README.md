# K4DD SMILES compatibility resume

The retained six-target case panel contains 487 rows. Eighteen retained rows require the documented `[N+H]`/`[N+H2]` spelling normalization for RDKit. The benchmark data under `data/` are unchanged. No checkpoint is retrained.
