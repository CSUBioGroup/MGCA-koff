# MoE baseline status

MoE retains its published/default configuration on its origin dataset, 2773.
For transfer to KinetX, the Stage 1/Stage 2 tuner is provided at
`ONLINE/cross_dataset_tuning/run_moe_kinetx_tuning.sh`.

The KinetX result also has partial valid-only coverage and is excluded from
best/second-best ranking in the table.
